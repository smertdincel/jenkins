apiVersion: v1
kind: Service
metadata:
  name: flask-ci-cd-service
spec:
  selector: { app: flask-ci-cd }
  type: NodePort
  ports:
  - protocol: TCP
    port: 80
    targetPort: 5000
    nodePort: 30080